{
  "name": "compile-tool-sass",
  "version": "0.0.1",
  "devDependencies": {
    "node-sass": "^3.4.2",
    "watch": "^0.17.1"
  },
  "scripts": {
    "compile:css": "node-sass --include-path scss sass/styles.scss css/styles.css",
    "compile": "npm run compile:css",
    "compile:sass": "watch 'npm run compile' sass/"
  }
}
