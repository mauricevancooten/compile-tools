{
  "name": "build-tool-sass-eyeglass",
  "version": "0.0.1",
  "devDependencies": {
    "breakpoint": "^0.1.6",
    "breakpoint-sass": "^2.7.0",
    "eyeglass": "^0.8.2",
    "node-sass": "^3.4.2",
    "singularitygs": "^1.7.0",
    "watch": "^0.17.1"
  },
  "scripts": {
    "build:css": "mkdir -p css && node scripts/eyeglass.js",
    "build": "npm run build:css",
    "build:watch": "watch 'npm run build' sass/"
  }
}
